<%= form_for(text_image) do |f| %>
  <% if text_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(text_image.errors.count, "error") %> prohibited this text_image from being saved:</h2>

      <ul>
      <% text_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.file_field :image %>


  <div class="actions">
    <%= f.submit "Upload" %>
  </div>
<% end %>
